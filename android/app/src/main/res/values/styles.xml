<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash/launch theme shown while Flutter engine boots (light mode).
         Must extend Theme.MaterialComponents so Stripe PaymentSheet can
         attach its Fragment to the Activity's FragmentManager. -->
    <style name="LaunchTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
    <!-- Applied once Flutter draws its first frame (light mode). -->
    <style name="NormalTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
